#--------------------------------------------------------------------------------------------------
# Adminer
  adminer:
    image: adminer:latest
    container_name: "${PROJECT_BASENAME}-adminer"
    restart: always
    ports:
      - "8088:8080"
    # Only if you need it.
    # environment:
    #   ADMINER_DESIGN: ${ADMINER_DESIGN}
